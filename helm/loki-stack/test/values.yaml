loki:
  singleBinary:
    persistence:
      enabled: true
      size: 10Gi
      storageClass: local-path

kube-prometheus-stack:
  grafana:
    adminUser: admin
    adminPassword: admin
    persistence:
      enabled: true              # no PVC, data resets on pod restart
      storageClass: local-path
      size: 1Gi
